name: CI

on:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v6

      - name: Verify formatting (fail if changes needed)
        run: dotnet format --verify-no-changes --severity warn

      - name: Run tests
        run: dotnet test
